# 狗场电脑1。用法：SITE=狗场1 ./record_multicam.sh
#
# 狗场从一台电脑拆成两台。这台负责 1/2/3 号单间——那三间的摄像头，
# 加那三间六个项圈（每只狗两个，轮换充电，两个都在这台上录）。
# 原来那份 sites/狗场.env **一个字没动**，一台机器录七路的老跑法随时能用。
#
# ════════════════════════════════════════════════════════════════════════
# ⚠ 开录之前必须做的一件事：核对摄像头索引
#
# 下面 CAMERAS 第二列是**这台机器上**的 /dev/video 索引（Windows 上是
# DirectShow 的枚举序号）。老的 sites/狗场.env 里那串 "4 0 3 5 2 1 6" 是**另一台
# 机器**上的枚举结果，换台机器、换个 USB 口就会变，照抄必错。
#
# 第二列已经在现场核对过了（2026-09-17）。除非重插 USB 口，不用再动。
# 要重新核对：
#
#     SITE=狗场1 ./record_multicam.sh --probe
#     PREVIEW=1 DEBUG=1 SITE=狗场1 ./record_multicam.sh   # 看画面认房间
#
# 画面上每一路都标着 cam1/cam2/cam3，对不上就改下面第二列的数字。
# 核对一次就好，之后除非重插 USB。
#
# 不核对的后果：不会报错，录一整天，然后 2 号房间的画面存成了 cam1——
# 平台按 camN 判房间，从此 A 房间的狗配着 B 房间的画面。跟设备编号按位置排
# 那个坑一模一样（那次是 imu2 里装着 5 号设备的数据）。
# ════════════════════════════════════════════════════════════════════════

SITE_ALIAS="gouchang1"

# ── 摄像头：机位号 → 这台机器上的索引 ────────────────────────────────────
# 第一列是**真实机位号**，进文件名（cam1_imu9_raw.mp4）。两台电脑共用一套
# 机位号，所以传到同一个 NAS 目录也不会撞——cam4 永远是 4 号单间，
# 不管它是哪台机器上的第几路。
CAMERAS="
1  2   # 1 号单间
2  0   # 2 号单间
3  1   # 3 号单间
"

# ── 设备：1/2/3 号房间的全部 6 个项圈 ────────────────────────────────────
#
# **按房间分，不按单双号分。** 每只狗两个项圈轮换充电（9/10 都是小白的），
# 两个都归这台机器管，所以不管它今天戴的是哪一个，都有 1 号单间的画面配着。
#
# 按单双号分（这台拿 9/11/13/15/17/19）看着更整齐，但那样的话 15/17/19 是
# 4/5/6 号房间的狗，摄像头在电脑2 上——于是"一只狗今天有没有视频，取决于
# 它今天戴的是单号还是双号项圈"。两台各 6 路蓝牙、各 3/4 路摄像头，
# 两种分法的负载完全一样，所以没有理由选那种。
DEVICES="
9   DC:32:39:07:0A:3D  xiaobai      # 小白 比熊        1 号单间
10  CF:FA:72:BE:48:76  xiaobai      # 小白 比熊        1 号单间（轮换的另一个）
11  CB:DC:DC:40:3D:D1  keji         # 流浪柯基         2 号单间
12  E0:33:21:2C:4B:C2  keji         # 流浪柯基         2 号单间（轮换的另一个）
13  EF:8A:A6:65:3A:BC  xiaojinmao   # 流浪小金毛 金毛   3 号单间
14  DD:CB:D4:26:F9:55  xiaojinmao   # 流浪小金毛 金毛   3 号单间（轮换的另一个）
"

# 这台不设 DEVICES_STANDBY：两个轮换组都在上面这张表里，一起录。
# USE_STANDBY 是"这次只录其中一组"用的开关，这里用不着。

# ── 配对 ────────────────────────────────────────────────────────────────
# 一间一狗一摄像头，所以一路只配它那间的两个项圈。全排列（3x6=18）出来大半是
# "A 房间的画面配 B 房间的狗"，纯废文件，而且每份都是一小时的 720p 拷贝。
PAIRS="cam1:imu9 cam1:imu10 \
       cam2:imu11 cam2:imu12 \
       cam3:imu13 cam3:imu14"

# 归档传 NAS 的清单：每路摄像头各点一次视频、每个设备各点一次 CSV。
#
# 轮换的第二个项圈只点 `:csv`——它配的是同一路摄像头的同一段画面，点成完整
# 配对的话那段视频要往 NAS 上传两遍（本地是硬链接不占地方，robocopy 走 SMB
# 是实打实的拷贝）。一小时 720p 约 500MB，六路就是每小时白传 1.5GB。
KEEP_PAIRS="cam1_imu9 cam2_imu11 cam3_imu13 \
            cam1_imu10:csv cam2_imu12:csv cam3_imu14:csv"

DAY_SUFFIX="_gouchang"

# 公用机位号（cleanup_gouchang.sh 读这个，不用每次手敲 --shared-cam）。
# 这台没有天花板那路（在电脑2 上）。清理脚本据此不去找 cam7。
SHARED_CAM="none"

CAPTURE_WIDTH="1280"
CAPTURE_HEIGHT="720"
EXTRA_ARGS="--no-save-overlay"
WARMUP_SEC="45"
OUT_DIR="data/multicam_multiimu"
