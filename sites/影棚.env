# 影棚。用法：SITE=影棚 ./record_multicam.sh
#
# 设备用名字指定（WT1、WT5…），跟机器上贴的标签、跟扫描打印出来的名字一致，
# 一眼就能核对。MAC 那一长串谁也记不住，抄错一位还查不出来。
#
# 名字曾经是危险的：早先 match_by_name 是子串匹配，有了 WT10 之后 `wit=WT1`
# 会同时命中 WT1 和 WT10，取哪个看谁的广播更新，每次重连都可能换一个而且一声
# 不吭。现在那里改成了「精确优先，含糊就报错」——名字完全相同就直接用，没有
# 精确命中而子串命中不止一个就直接报错让人写全名或 MAC，绝不猜。所以名字安全了。
#
# MAC 仍然认（`wit=D1:FD:A8:C7:1A:EF`），设备改了名字又还没来得及更新配置时用得上。
#
# 影棚 8 个设备、4 只狗，每只狗两个轮换充电。下面这组是"当班"的四个，
# 换班时把 IMUS 里对应那一项换成备用那个（同一行注释里写着）。
#
# ⚠ IMUS 的顺序决定文件名里的 imu1/imu2/imu3/imu4，别随便调换顺序——
#   调了顺序就等于把数据记到别的狗名下。换设备只换 MAC，位置不动。

# imu1 = bibi   当班 WT1  D1:FD:A8:C7:1A:EF   备用 WT2  EE:96:8A:A4:C6:2A
# imu2 = bali   当班 WT5  F0:76:DC:BE:E2:CA   备用 WT6  D5:34:E2:B9:6F:32
# imu3 = lulu   当班 WT8  D9:01:5C:F2:48:E5   备用 WT7  （MAC 待补，扫描时没见到）
# imu4 = xima   当班 WT3  C0:05:B7:EF:26:32   备用 WT4  E5:F9:02:82:B8:E4
#
# ════════════════════════════════════════════════════════════════════════
# ⚠ 上面这张表还没有得到确认，别拿它当事实用，也先别照着改成 DEVICES 表。
#
# 三件已知的事：
#
# 1) 影棚现在还不是严格按编号佩戴的——哪只狗戴哪个设备，当天拿到哪个戴哪个。
#    所以"imu2 = bali"这种话现在没有稳定含义。狗场那边已经是固定对应了
#    （DEVICES 表，一行「真实编号 MAC 狗名」），影棚要等佩戴规范定下来才能照搬。
#
# 2) 上面这张表跟平台狗档案登记的连号配对对不上：
#       这里说  bali=WT5/6   lulu=WT8/7   xima=WT3/4
#       档案说  巴利=IMU3+4  lulu=IMU5+6  小满=IMU7+8
#    只有 bibi=1/2 两边一致。在佩戴规范定下来之前，这个分歧不去猜。
#
# 3) 因为没设 IMU_IDS，文件名里的 imu1..imu4 是按 IMUS 的**位置**排的，
#    不是设备真实编号。2026-09-10 现场实测：位置 2 上的设备其实是 WT5。
#    也就是说现在录出来的文件名，imu 号和真实设备号对不上。
#
#    没有当场改成真实编号，是因为 KEEP_PAIRS 也是按 imu 号挑要传 NAS 的文件
#    （下面那行 cam1_imu1 cam2_imu2 …）。只改 IMU_IDS 不改 KEEP_PAIRS，
#    归档会一个文件都挑不出来，而且不报错——比现在的编号不准更糟。
#    两处要一起改，而每次换班 IMU_IDS 和 KEEP_PAIRS 都得跟着动，
#    这正是 DEVICES 表要解决的问题，所以并到同一次改。
#
# 现场观测记录（2026-09-10，公司影棚）：
#    在广播:   WT2 WT4 WT5 WT6 WT8
#    没扫到:   WT1 WT3 WT7
#    配置里当班的 WT1、WT3 都不在，说明当天换过班；WT5 和 WT6 同时在广播，
#    光看"能不能扫到"分不出哪个戴在狗身上（充电中的设备也开着、也广播）。
#
# 佩戴规范定下来之后要做的（一次做完）：
#    a. 补齐 WT7 的 MAC（历次扫描都没见到）
#    b. 确认 WT↔狗 的对应，用 DEBUG=1 边晃项圈边看画面上哪个 imu 的 Hz 在动
#    c. 改成 DEVICES 表（真实编号 MAC 狗名，备用写同一行），跟狗场一致
#    d. KEEP_PAIRS 跟着换成真实编号
# ════════════════════════════════════════════════════════════════════════
# 这个场地的 ASCII 别名。SITE=gouchang 和 SITE=狗场 等价。
# 为什么要有：install_autostart.bat 是 .bat，而 cmd 是按字节读 .bat 的，
# 里面出现中文早晚出乱子（setup_windows.bat 就栽过一次）。有了别名，.bat 全程
# 只碰 ASCII，中文只出现在 bash 读的文件里。
SITE_ALIAS="yingpeng"

IMUS="wit=WT1 wit=WT5 wit=WT8 wit=WT3"
DOG_NAMES="bibi bali lulu xima"

# 影棚 3 路摄像头
CAMS="0 1 2"

# 这里故意不设 PAIRS：影棚是一个大空间、3 个摄像头都可能拍到 4 只狗中的任意
# 一只，哪路摄像头拍到哪只狗事先不知道，所以全排列（3x4=12 对）是有意义的。
# 狗场那种一间一狗一摄像头的场地才需要设 PAIRS 限制。

# 归档时要传 NAS 的配对（daily_archive.sh 读这个）
KEEP_PAIRS="cam1_imu1 cam2_imu2 cam3_imu3 cam1_imu4:csv"

# 日期目录的后缀：2026_9_9_yingpeng（本地和 NAS 上是同一个名字）。
# 两个场地各自往同一个 NAS 传，不加后缀的话日期目录会混在一起，想确认这个场地
# 今天传全了没有只能自己按 imu 号挑。
# 用拼音不用中文：这个名字要一路穿过 cygpath -w → robocopy → SMB → 后端容器的
# 挂载点，中间任何一环编码没对齐就是乱码目录，而且几天后才会从平台上发现。
DAY_SUFFIX="_yingpeng"

OUT_DIR="data/multicam_multiimu"
