# 影棚。用法：SITE=影棚 ./record_multicam.sh
#
# 设备用名字指定（WT1、WT5…），跟机器上贴的标签、跟扫描打印出来的名字一致，
# 一眼就能核对。MAC 那一长串谁也记不住，抄错一位还查不出来。
#
# 名字曾经是危险的：早先 match_by_name 是子串匹配，有了 WT10 之后 `wit=WT1`
# 会同时命中 WT1 和 WT10，取哪个看谁的广播更新，每次重连都可能换一个而且一声
# 不吭。现在那里改成了「精确优先，含糊就报错」——名字完全相同就直接用，没有
# 精确命中而子串命中不止一个就直接报错让人写全名或 MAC，绝不猜。所以名字安全了。
#
# MAC 仍然认（`wit=D1:FD:A8:C7:1A:EF`），设备改了名字又还没来得及更新配置时用得上。
#
# 影棚 8 个设备、4 只狗，每只狗两个轮换充电。设备表在下面（DEVICES /
# DEVICES_STANDBY），换班时把两个块的内容对调，变量名不动。
#
# ════════════════════════════════════════════════════════════════════════
# ⚠ 已经解决的 和 还没解决的，分清楚：
#
# 已解决（2026-09-12）：文件名里的 imu 号现在是**设备真实编号**（WT5 就是 5），
#   不再是"排第几个"。改之前 imu2 里装的是 5 号设备的数据，平台按号查狗就查错。
#
# 也解决了（2026-09-13 现场确认）：**哪只狗戴哪个设备**。就是连号的：
#     bibi = 1,2    bali = 3,4    lulu = 5,6    xima = 7,8
#   跟平台狗档案登记的一致（档案：bibi=IMU1+2、巴利=IMU3+4、lulu=IMU5+6、
#   小满=IMU7+8）。本文件原来那张表是错的（把 3/4 记成 xima、5/6 记成 bali、
#   7/8 记成 lulu），已按现场为准改掉。
#
# ⚠ 还错着、但不在这个仓库里：label_service/skin_rules.py 的 IMU_DOG_DEFAULT_MAP
#   写的是 IMU1=比熊-BB、IMU2=金毛-巴利、IMU3=中华田园犬-露露、IMU4=马尔济斯-小满，
#   按位置排的，跟上面对不上。它只是 dog_name_service 查不到 dogs 档案时的兜底，
#   档案齐全就不会生效——但档案哪天缺一条，它就会静悄悄给出错的狗。
#   （那个文件由 pm_skin_scoring/code/questionnaire_app.py 生成，要改改源头再同步。）
#
# 还有一件没定：当班/备用怎么轮。下面 DEVICES 里当班那组是沿用改编号之前的
# 选择（1/3/5/8），不是"单数当班双数备用"。哪个真戴在狗身上，扫描分不出来
# （充电中的设备也在广播）。ALL_DEVICES=1 两个都录，这件事就不重要了。
#
# 3) 编号问题已于 2026-09-12 改掉（下面的 DEVICES 表）。留着这段是因为它解释了
#    9-11 及以前的影棚数据该怎么读：
#
#    那时没设 IMU_IDS，文件名里的 imu1..imu4 是按 IMUS 的**位置**排的，不是
#    设备真实编号。2026-09-10 现场实测位置 2 上的是 WT5。所以历史文件：
#        imu1 ← WT1     imu2 ← WT5     imu3 ← WT8     imu4 ← WT3
#    而平台是拿文件名里的号去狗档案查狗的，号对不上设备，归属就跟着错。
#    要复用这批历史数据，得先按上面这张对照把号翻译过来。
#
# 现场观测记录（2026-09-10，公司影棚）：
#    在广播:   WT2 WT4 WT5 WT6 WT8
#    没扫到:   WT1 WT3 WT7
#    配置里当班的 WT1、WT3 都不在，说明当天换过班；WT5 和 WT6 同时在广播，
#    光看"能不能扫到"分不出哪个戴在狗身上（充电中的设备也开着、也广播）。
#
# 佩戴规范定下来之后要做的（一次做完）：
#    a. 补齐 WT7 的 MAC（历次扫描都没见到）。其实不补也能用——名字匹配现在是
#       「精确优先，含糊就报错」，wit=WT7 不会被 WT1 之类抢走。补上只是为了
#       设备哪天被改了名还能认出来。
#    b. 确认 WT↔狗 的对应。
#       ⚠ 原来这里写的是「用 DEBUG=1 边晃项圈边看画面上哪个 imu 的 Hz 在动」，
#         这个办法不成立：Hz 是 BLE 推送频率，由设备自己的采样率决定，晃不晃
#         都一样；画面上那一行（imu_report）也只画 Hz，不画运动量。
#       正确做法——一次晃一只狗的项圈，跑 8 秒看哪个报「有活动」，四只各来一次：
#         python check_device_worn.py --imu wit=WT1 --imu wit=WT5 \
#             --imu wit=WT8 --imu wit=WT3 --duration 8
#    c. ✅ 已做：改成 DEVICES 表（真实编号 名字 狗名），跟狗场一致
#    d. ✅ 已做：KEEP_PAIRS 跟着换成真实编号
#
# ✅ 2026-09-13 定了：设备归属就是连号的 bibi=1,2 bali=3,4 lulu=5,6 xima=7,8，
#   跟 dogs 档案一致。本文件那张旧表是错的，已改。
#
# ⚠ 9-11 及以前的影棚数据，有一半的归属是错的。按当时的位置编号：
#     文件 imu1 装的是 WT1（bibi 的）→ 平台判给 bibi  ✓
#     文件 imu2 装的是 WT5（lulu 的）→ 平台判给 bibi  ✗  lulu 的数据混进了 bibi
#     文件 imu3 装的是 WT8（xima 的）→ 平台判给 bali  ✗  xima 的数据混进了 bali
#     文件 imu4 装的是 WT3（bali 的）→ 平台判给 bali  ✓
#   所以 bibi 和 bali 的历史统计里各混了另一只狗的数据，而 lulu 和 xima 的
#   历史数据整个不见了（被记在别人名下）。
#
#   ⚠ 而且这张对照只对「当班那组一直是 WT1/WT5/WT8/WT3」的日子成立。9-10 那天
#     实测 WT1 和 WT3 根本不在场，说明换过班——那天的位置编号对应的是别的设备。
#     要按天恢复，去翻 logs/record_multicam_<日期>.log：开录前预检会打印
#     「imu1 = WT1(D1:FD:...)」这样的对照，那是每一天的真实记录。
# ════════════════════════════════════════════════════════════════════════
# 这个场地的 ASCII 别名。SITE=gouchang 和 SITE=狗场 等价。
# 为什么要有：install_autostart.bat 是 .bat，而 cmd 是按字节读 .bat 的，
# 里面出现中文早晚出乱子（setup_windows.bat 就栽过一次）。有了别名，.bat 全程
# 只碰 ASCII，中文只出现在 bash 读的文件里。
SITE_ALIAS="yingpeng"

# ── 设备表（2026-09-12 从 IMUS 位置编号改过来）────────────────────────
#
# 改之前：IMUS="wit=WT1 wit=WT5 wit=WT8 wit=WT3"，没有 IMU_IDS，所以文件名里的
# imu 号是按**位置**排的 imu1..imu4。9-10 现场实测位置 2 上的是 WT5，也就是
# 文件名写着 imu2、里面装的是 5 号设备的数据。平台是拿文件名里的号去狗档案
# 查是哪只狗的，号对不上设备，归属就是错的——这个错今天就在发生，跟全不全采无关。
#
# 改之后：第一列是设备真实编号（WT5 就是 5，跟狗场一个规矩）。编号和名字写在
# 同一行、而且看得出来对不对（"5  WT5"），抄错一眼就能发现。
#
# 第二列可以写名字也可以写 MAC。这里用名字：match_by_name 现在是「精确优先，
# 含糊就报错」，WT1 不会被 WT10 抢走；WT7 的 MAC 一直没扫到，用名字就不卡这一项。
# 设备哪天被改了名再换回 MAC（在上面那张表里）。
#
# ⚠ 狗名这一列只画在画面上给人看，不决定数据算在哪只狗头上——那个由平台
#   狗档案的 IMU 登记决定。而档案跟这里现在是矛盾的（见下面的对照），
#   所以光改这个文件不够，档案那边必须一起定。
DEVICES="
1  WT1  bibi     # D1:FD:A8:C7:1A:EF
3  WT3  bali     # C0:05:B7:EF:26:32
5  WT5  lulu     # F0:76:DC:BE:E2:CA
8  WT8  xima     # D9:01:5C:F2:48:E5
"

# 备用那四个（充电轮换的另一组）。换班时把两个块的内容对调，变量名不动。
#
# WT7 历次扫描都没见到，跑起来多半会在预检里报"未找到"——那本身就是要查的事。
DEVICES_STANDBY="
2  WT2  bibi     # EE:96:8A:A4:C6:2A
4  WT4  bali     # E5:F9:02:82:B8:E4
6  WT6  lulu     # D5:34:E2:B9:6F:32
7  WT7  xima     # MAC 一直没扫到，用名字；2026-09-13 扫描里也没有它
"

# ── 一次采 8 个（当班 + 备用一起）─────────────────────────────────────
# 先测蓝牙顶不顶得住：
#   SITE=影棚 ONLY_ON_DUTY=1 ./check_ble_capacity.sh   # 4 个，基准
#   SITE=影棚 ./check_ble_capacity.sh                  # 8 个
# 测下来没问题，录制时加 ALL_DEVICES=1（或者打开下面这行）：
# ALL_DEVICES="1"

# 影棚 3 路摄像头
CAMS="0 1 2"

# 这里故意不设 PAIRS：影棚是一个大空间、3 个摄像头都可能拍到 4 只狗中的任意
# 一只，哪路摄像头拍到哪只狗事先不知道，所以全排列（3x4=12 对）是有意义的。
# 狗场那种一间一狗一摄像头的场地才需要设 PAIRS 限制。

# 归档时要传 NAS 的配对（daily_archive.sh 读这个）
# 一路摄像头只传一份视频（三路各一份），其余设备只传 CSV——影棚是全排列
# 3x8=24 份配对，全传会把同一段画面传 8 遍。
# 后 4 条是备用设备（ALL_DEVICES=1 时才有对应文件）；常驻在这里是安全的，
# want_file 按关键字子串挑文件，没匹配到的静默跳过。
KEEP_PAIRS="cam1_imu1 cam2_imu3 cam3_imu5 cam1_imu8:csv \
            cam1_imu2:csv cam1_imu4:csv cam1_imu6:csv cam1_imu7:csv"

# 日期目录的后缀：2026_9_9_yingpeng（本地和 NAS 上是同一个名字）。
# 两个场地各自往同一个 NAS 传，不加后缀的话日期目录会混在一起，想确认这个场地
# 今天传全了没有只能自己按 imu 号挑。
# 用拼音不用中文：这个名字要一路穿过 cygpath -w → robocopy → SMB → 后端容器的
# 挂载点，中间任何一环编码没对齐就是乱码目录，而且几天后才会从平台上发现。
DAY_SUFFIX="_yingpeng"

OUT_DIR="data/multicam_multiimu"
