# 狗场。用法：SITE=狗场 ./record_multicam.sh
#
# 设备一律用 MAC 指定，不用名字。名字是子串匹配，而这里有 WT9…WT20——
# 写名字的话 "WT1" 会同时命中 WT1/WT10…WT19，取哪个看谁的广播更新，每次可能
# 不一样且一声不吭（见 ble_utils.match_by_name）。MAC 是精确的，改名也不影响。
#
# 狗场 6 只狗、12 个设备（每只两个轮换充电）、6 路摄像头。
# 配对按狗档案里登记的来，是连号的：IMU9+10 一只、11+12 一只，以此类推。
#
# ⚠ IMUS 的顺序决定文件名里的 imu1..imu6，别调换顺序——调了就等于把数据记到
#   别的狗名下。换班只换 MAC，位置不动。

# imu1 = 小白       比熊    当班 WT9  DC:32:39:07:0A:3D   备用 WT10 CF:FA:72:BE:48:76
# imu2 = 流浪柯基   柯基    当班 WT11 CB:DC:DC:40:3D:D1   备用 WT12 E0:33:21:2C:4B:C2
# imu3 = 流浪小金毛 金毛    当班 WT13 EF:8A:A6:65:3A:BC   备用 WT14 DD:CB:D4:26:F9:55
# imu4 = 旺财       法斗    当班 WT15 CE:6E:61:15:B7:6E   备用 WT16 CB:38:8F:08:6A:5A
# imu5 = 大金毛     金毛    当班 WT17 F7:26:19:3A:AB:6F   备用 WT18 DC:27:13:E4:42:37
# imu6 = 东德       东德    当班 WT19 E4:A5:52:E8:DC:7E   备用 WT20 D4:D4:A4:9D:8F:41
IMUS="wit=DC:32:39:07:0A:3D wit=CB:DC:DC:40:3D:D1 wit=EF:8A:A6:65:3A:BC wit=CE:6E:61:15:B7:6E wit=F7:26:19:3A:AB:6F wit=E4:A5:52:E8:DC:7E"

# 只影响画面上叠加显示的名字，不进文件名。
# 必须是 ASCII：画面用 OpenCV 的 Hershey 字体画字，那套字体没有中文字形，
# 写"小白"出来是一串方块或问号，等于白写。所以用拼音，顺序跟 IMUS 一一对应。
DOG_NAMES="xiaobai keji xiaojinmao wangcai dajinmao dongde"

# 狗场 6 路摄像头
CAMS="0 1 2 3 4 5"

# 一间一狗一摄像头：cam_i 只拍得到 imu_i 那只狗，所以只配这 6 对。
# 不设的话是全排列 6x6=36 份配对，其中 30 份是「A 房间的画面配 B 房间的狗」，
# 纯废文件——而且每份都是一小时的 720p 视频拷贝，磁盘按 6 倍烧。
PAIRS="cam1:imu1 cam2:imu2 cam3:imu3 cam4:imu4 cam5:imu5 cam6:imu6"

# 归档时要传 NAS 的配对，跟 PAIRS 一一对应（daily_archive.sh 读这个）
KEEP_PAIRS="cam1_imu1 cam2_imu2 cam3_imu3 cam4_imu4 cam5_imu5 cam6_imu6"

OUT_DIR="data/multicam_multiimu"
