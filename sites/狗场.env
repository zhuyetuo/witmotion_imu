# 狗场。用法：SITE=狗场 ./record_multicam.sh
#
# 这里用 MAC。名字也能用（match_by_name 现在是「精确优先，含糊就报错」，
# WT1 不会被 WT10 抢走），但狗场这 12 个设备是长期固定的，MAC 抄一次就不用再动，
# 而且设备万一被改了名也不受影响。影棚那边设备常换，用的是名字，两种都支持。
#
# 狗场 6 只狗、12 个设备（每只两个轮换充电）、6 路摄像头。
# 配对按狗档案里登记的来，是连号的：IMU9+10 一只、11+12 一只，以此类推。
#
# ⚠ IMUS 的顺序决定文件名里的 imu1..imu6，别调换顺序——调了就等于把数据记到
#   别的狗名下。换班只换 MAC，位置不动。

# 一行一个设备：真实编号  MAC  画面上显示的名字
#
# 编号是设备本身的号（WT9 就是 9），不是"排第几个"。位置序号会漂——调换顺序，
# 同一个 imu1 就成了另一台设备，而文件名看不出任何差别；两个场地也会各自从
# imu1 开始撞车。平台是按这个号认是哪只狗的（狗档案登记的就是 IMU1..IMU20）。
#
# 写成一张表而不是三个平行数组，是因为平行数组要靠人手工对齐，错位一格不报错，
# 只是从此以后 A 狗的数据记在 B 狗名下。编号、MAC、狗名本来就该在同一行。
#
# 名字必须是 ASCII：画面上的字用 OpenCV 的 Hershey 字体画，那套字体没有中文
# 字形，写"小白"出来是一串方块。所以用拼音。
#
# 每只狗两个设备轮换充电，下面是当班的那一个；换班把 MAC 换成备用那个的，
# 编号也跟着换成备用的号（编号跟设备走，不跟狗走）。
#                                        备用
# 这个场地的 ASCII 别名。SITE=gouchang 和 SITE=狗场 等价。
# 为什么要有：install_autostart.bat 是 .bat，而 cmd 是按字节读 .bat 的，
# 里面出现中文早晚出乱子（setup_windows.bat 就栽过一次）。有了别名，.bat 全程
# 只碰 ASCII，中文只出现在 bash 读的文件里。
SITE_ALIAS="gouchang"

# ── 当班设备（现在用的这组）─────────────────────────────────────────
# 换班就是把这一组注释掉、把下面那组的注释去掉，两组来回切，不用改数字。
# 编号跟设备走不跟狗走：小白今天戴 9 号、明天戴 10 号，文件名里就是 imu9/imu10，
# 平台按这个号认狗（狗档案里 9 和 10 都登记给小白）。
DEVICES="
9   DC:32:39:07:0A:3D  xiaobai      # 小白 比熊
11  CB:DC:DC:40:3D:D1  keji         # 流浪柯基
13  EF:8A:A6:65:3A:BC  xiaojinmao   # 流浪小金毛 金毛
15  CE:6E:61:15:B7:6E  wangcai      # 旺财 法斗
17  F7:26:19:3A:AB:6F  dajinmao     # 大金毛 金毛
19  E4:A5:52:E8:DC:7E  dongde       # 东德
"

# ── 备用设备（充电轮换的另一组）──────────────────────────────────────
# 换班时：给上面那组每行前面加 #，把这一组每行前面的 # 去掉。
# 别两组同时打开——后读到的会把前面的整个覆盖掉，而且一声不吭。
# DEVICES="
# 10  CF:FA:72:BE:48:76  xiaobai      # 小白 比熊
# 12  E0:33:21:2C:4B:C2  keji         # 流浪柯基
# 14  DD:CB:D4:26:F9:55  xiaojinmao   # 流浪小金毛 金毛
# 16  CB:38:8F:08:6A:5A  wangcai      # 旺财 法斗
# 18  DC:27:13:E4:42:37  dajinmao     # 大金毛 金毛
# 20  D4:D4:A4:9D:8F:41  dongde       # 东德
# "


# 狗场 6 路摄像头
# 前 6 个是每间一只狗，顺序是现场一个个认出来的（系统给的设备号跟房间号没关系）。
# 第 7 个吊在天花板上拍全场，6 只狗都拍得到——用来补小单间的死角。
# 如果第七路插上去不是设备 6，用 ./debug_cameras.sh 看画面认一下再改这里。
CAMS="3 1 0 5 2 4 6"

# 天花板那路是倒着装的，转 180° 才是正的。
# 转在采集这一步，录进视频的就是转好的——标注、AI 推理、导出看到的都一致。
ROTATE="cam7:180"

# 一间一狗一摄像头：cam_i 只拍得到 imu_i 那只狗，所以只配这 6 对。
# 不设的话是全排列 6x6=36 份配对，其中 30 份是「A 房间的画面配 B 房间的狗」，
# 纯废文件——而且每份都是一小时的 720p 视频拷贝，磁盘按 6 倍烧。
# 每只狗两路画面：自己单间那路 + 天花板那路公共的。
# cam7 跟 6 只狗都配，配对文件用硬链接生成，不会多占磁盘（见 _link_or_copy）。
PAIRS="cam1:imu9 cam2:imu11 cam3:imu13 cam4:imu15 cam5:imu17 cam6:imu19 \
       cam7:imu9 cam7:imu11 cam7:imu13 cam7:imu15 cam7:imu17 cam7:imu19"

# 归档时要传 NAS 的配对，跟 PAIRS 一一对应（daily_archive.sh 读这个）
# 归档要传 NAS 的配对，跟 PAIRS 一一对应。
# ⚠ 传到 NAS 是真拷贝（robocopy 读的是内容，硬链接在那边还原成独立文件），
#   所以 cam7 那 6 份在 NAS 上会占 6 份空间。先这么传，嫌大再砍成只留几只狗的。
KEEP_PAIRS="cam1_imu9 cam2_imu11 cam3_imu13 cam4_imu15 cam5_imu17 cam6_imu19 \
            cam7_imu9 cam7_imu11 cam7_imu13 cam7_imu15 cam7_imu17 cam7_imu19"

# 日期目录的后缀：2026_9_9_gouchang（本地和 NAS 上是同一个名字）。
# 两个场地各自往同一个 NAS 传，不加后缀的话日期目录会混在一起，想确认这个场地
# 今天传全了没有只能自己按 imu 号挑。
# 用拼音不用中文：这个名字要一路穿过 cygpath -w → robocopy → SMB → 后端容器的
# 挂载点，中间任何一环编码没对齐就是乱码目录，而且几天后才会从平台上发现。
DAY_SUFFIX="_gouchang"

# ── 下面几项是在这台机器上实测定下来的（i5-10400 + UHD 630，6 路 720p）──

# 直接按 720p 采，不要 1080p 再缩：省掉每帧一次缩放，USB 带宽也降一半多。
#
# 曾经怀疑这样会丢视野——广角摄像头被要求 720p 时，有些驱动不是等比缩小，而是
# 从 1080p 传感器中间裁一块 1280x720 出来。所以改成 1080p 采集试过一轮。
#
# 现场用 DEBUG=1 两种设置各跑一次、对着同一个画面比过边缘：**没有区别**。
# 这批摄像头的驱动是等比缩小的，不裁切。所以还原成直接采 720p，白拿带宽和 CPU。
#
# 别再改回 1920x1080 了，除非换了摄像头。真要复验：
#   CAPTURE_WIDTH=1920 CAPTURE_HEIGHT=1080 DEBUG=1 SITE=狗场 ./record_multicam.sh
#   CAPTURE_WIDTH=1280 CAPTURE_HEIGHT=720  DEBUG=1 SITE=狗场 ./record_multicam.sh
CAPTURE_WIDTH="1280"
CAPTURE_HEIGHT="720"

# 预览窗口默认是关的（PREVIEW 的默认值就是 0，这里不用管）。
# 录制的常态是无人值守跑一整晚，没人看那些窗口，而七路 720p 的 imshow 加 waitKey
# 每 tick 吃掉 30 多毫秒，白掉四成帧率。
# 想看一眼：在跑着的终端里敲 p + 回车 调出来，看完再敲一次关掉。
# 只想认摄像头、不录数据：跑 ./debug_cameras.sh（不落盘，随开随关）。
# 停止录制敲 q + 回车，或者 Ctrl-C（ffmpeg 已做进程组隔离，两种都能正常收尾）。
EXTRA_ARGS=""

# 预热 45 秒，不是默认的 10 秒。
# 6 个 BLE 设备在一个适配器上排队连接，10 秒只连得上两三个——剩下的会在录制
# 开始后才陆续连上，于是每小时那一段的开头几十秒，那几只狗的 IMU 是空的。
# 实测 6 个全连上大概要 30~40 秒。预热多花的这半分钟，换的是每段开头都完整。
WARMUP_SEC="45"

OUT_DIR="data/multicam_multiimu"
