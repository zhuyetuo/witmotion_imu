# 采集/分析用到的第三方包。setup_windows.bat 按这个装。
#
# 为什么钉上限而不是随便装最新：换台机器装出不同版本，行为就不一样，
# 而这类问题排查起来极其费时（"同样的代码在那台机器上好好的"）。
#
# opencv-python 卡在 4.x 是有具体原因的：5.0 的 Hershey 字体渲染跟 4.x 不一样，
# 画面叠加的那几行字会糊成一团（同样的 putText 调用，4.x 正常、5.0 重叠）。
# 录像上的时间戳和设备号糊了，事后核对就没依据了。
bleak>=0.22,<1.0
opencv-python>=4.8,<5.0
numpy>=1.24,<3.0
pandas>=2.0,<3.0
scipy>=1.10,<2.0
